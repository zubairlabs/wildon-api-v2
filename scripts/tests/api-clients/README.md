# API Clients Test Runner

Run focused tests for API-client related services:

```bash
scripts/tests/api-clients/run.sh
```

Run full workspace tests:

```bash
scripts/tests/api-clients/run.sh workspace
```

Default mode is `focused`.
