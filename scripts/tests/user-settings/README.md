# User Settings Test Runner

Run focused user-settings related tests:

```bash
scripts/tests/user-settings/run.sh
```

Run full workspace tests:

```bash
scripts/tests/user-settings/run.sh workspace
```

Default mode is `focused`.
